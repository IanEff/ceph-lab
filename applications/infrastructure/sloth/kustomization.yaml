apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
  - prometheusservicelevels.yaml

helmCharts:
  - name: sloth
    repo: https://slok.github.io/sloth
    version: 0.16.0
    releaseName: sloth
    namespace: monitoring
    includeCRDs: true
    valuesFile: values.yaml
