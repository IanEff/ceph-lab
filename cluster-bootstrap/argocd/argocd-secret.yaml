# DEV/LAB ONLY — default password is "password"
# Change via: argocd account update-password
# Or via the UI after first login.
apiVersion: v1
kind: Secret
metadata:
  name: argocd-secret
  namespace: argocd
type: Opaque
stringData:
  # bcrypt hash of "password" — update before sharing this repo
  admin.password: "$2y$05$/DI4jDEYr4GfgfrGeb7JHeuMyylLsgTb5mfz1vPxUtKz5d1jQgBhW"
  admin.passwordMtime: "2026-01-01T00:00:00Z"
